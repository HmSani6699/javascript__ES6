//declare the arrow function has one parameter divisible 5
const divisible = num => num / 5;
// console.log(divisible(13))


const sum = (num1, num2) => (num1 + 2) * (num2 + 2);

// console.log(sum(5, 5));


const multiply = (num1, num2, num3) => num1 * num2 * num3;
// console.log(multiply(2, 2, 2));


const sum1 = (num1, num2) => {
    const first = num1 + 2;
    const second = num2 + 2;

    const multiply = first * second;
    return multiply
};

// console.log(sum1(5, 5))